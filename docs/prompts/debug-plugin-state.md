Investigate hosted-plugin state restore in the Soundshed Guitar repo, focusing on why a hosted VST3 may save successfully but reload with stale or incorrect runtime state. Start from the actual control path that applies hosted plugin state, not broad repo exploration. Trace the flow across UI, preset serialization, controller rehydration, graph application, and JUCE hosted-plugin restore. Use the existing instrumentation and debug surfaces first, especially the redacted snapshot file at C:\Users\chris\AppData\Roaming\Soundshed Guitar\data\v1\logs\debug-state.json and the session log alongside it, plus the hosted-plugin logging in core/src/PluginController.cpp and juce/source/JuceHostedPluginEffect.cpp. Compare pluginStateBase64 presence, encoded blob hashes, parameter fingerprint logs, and whether setStateInformation() is being invoked on the final prepared plugin instance. Assume the failure mode may be “correct blob captured, wrong blob later rehydrated” or “correct blob applied before final instance replacement.” Form one falsifiable local hypothesis, run the cheapest discriminating check, and only then propose or make the smallest fix. Prioritize Graphene-style regressions and reuse any nearby tests that already cover hosted-plugin state capture/restore. Report: 1. the exact failing control point, 2. evidence from logs or code proving it, 3. the minimal fix, and 4. the narrowest validation command or test that confirms the fix.